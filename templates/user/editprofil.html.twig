{% extends 'base.html.twig' %}

{% block title %}modification utilisateur{% endblock %}
{% block breadcrumbs %}
<script src="{{ asset('theme/assets/js/jquery.min.js')}}"></script>

        <div class="row">
                            <div class="col-12">
                                <div class="page-title-box d-flex align-items-center justify-content-between">
                                    <h4 class="mb-0 font-size-18">Profil {{app.user.username}}</h4>

                                    <div class="page-title-right">
                                        <ol class="breadcrumb m-0">
                                            <li class="breadcrumb-item"><a href="javascript: void(0);">Gestion Film</a></li>
                                            <li class="breadcrumb-item"><a href="javascript: void(0);">Profil {{app.user.username}}</a></li>
                                            <li class="breadcrumb-item active">Modification</li>
                                        </ol>
                                    </div>

                                </div>
                            </div>
                        </div>
                        <hr/>
                        {% endblock %}
{% block body %}


         <div class="row">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-body">

     <div class="row">
                            <div class="col-8">
                                        <h4 class="card-title"><i class="fa fa-angle-right"></i>Modifier Profil {{app.user.username}}</h4>
</div><div class="col-4">
                                            <a href="{{ path('profil') }}" class="btn btn-secondary">Retour au profil</a>

                                   </div>
                               </div>

    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
  </div></div></div></div>
{% endblock %}
