{{ form_start(form) }}
    {{ form_widget(form) }}
    <br>
   <div class="row">
   <div class="col-6"> <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button></div>
   </div>
{{ form_end(form) }}

