
{% extends 'base.html.twig' %}

{% block title %}Détails tilisateur{% endblock %}
{% block breadcrumbs %}
<script src="{{ asset('theme/assets/js/jquery.min.js')}}"></script>

        <div class="row">
                            <div class="col-12">
                                <div class="page-title-box d-flex align-items-center justify-content-between">
                                    <h4 class="mb-0 font-size-18">Client</h4>

                                    <div class="page-title-right">
                                        <ol class="breadcrumb m-0">
                                            <li class="breadcrumb-item"><a href="javascript: void(0);">Gestion film</a></li>
                                            <li class="breadcrumb-item"><a href="javascript: void(0);">Client </a></li>
                                            <li class="breadcrumb-item active">Détails</li>
                                        </ol>
                                    </div>

                                </div>
                            </div>
                        </div>
                        <hr/>
                        {% endblock %}
{% block body %}


         <div class="row">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-body">

     <div class="row">
                            <div class="col-8">
                                        <h4 class="card-title"><i class="fa fa-angle-right"></i>Détails {{ user.nom }} {{ user.prenom }}</h4>
</div>
                               </div>
<br><br>
<div class="table-responsive">
                                            <table class="table table-editable">
        <tbody>

   <tr>
                <th>Nom  </th>
                <td>{{ user.nom }}</td>
            </tr>

            <tr>
                <th>Prénom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
  <tr>
                <th>Nom utilisateur</th>
                <td>{{ user.username }}</td>
            </tr>
        </tbody>
    </table>
    <button class="btn btn-primary"><a href="{{path('editprofil')}}" style="color:white">Modifier</a></button>
    </div>
  </div></div></div></div>
{% endblock %}

